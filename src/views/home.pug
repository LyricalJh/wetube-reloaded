extends base.pug
include mixins/video

block content
    div.video-grid
        each video in videos 
            +video(video)
        else 
            span.empty_message No video found
block scripts
    script(src="/static/js/cross.js")